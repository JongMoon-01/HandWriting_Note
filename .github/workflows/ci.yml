name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-unit-tests:
    name: Backend module unit tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        module: [document, annotation, user, export]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      # TODO: replace with real Gradle/Maven module test command once backend/ scaffolding lands
      - name: Run ${{ matrix.module }} module tests
        run: echo "TODO: ./gradlew :backend:${{ matrix.module }}:test"

  worker-unit-tests:
    name: Go worker unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Run worker tests
        run: echo "TODO: go test ./worker/..."

  integration-tests:
    name: Full service integration test
    runs-on: ubuntu-latest
    needs: [backend-unit-tests, worker-unit-tests]
    steps:
      - uses: actions/checkout@v4
      - name: Run integration suite
        run: echo "TODO: docker-compose based integration test run"

  build-and-push:
    name: Build & push images to GHCR
    runs-on: ubuntu-latest
    needs: [integration-tests]
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & push backend image
        run: echo "TODO: docker build/push backend"
      - name: Build & push worker image
        run: echo "TODO: docker build/push worker"

  deploy:
    name: Deploy to k3s (Vultr)
    runs-on: ubuntu-latest
    needs: [build-and-push]
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy manifests
        run: echo "TODO: kubectl apply -f infra/k8s (via SSH or self-hosted runner on the k3s box)"
